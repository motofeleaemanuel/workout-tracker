import styled from "styled-components";
import { SIZES } from "../../theme/theme";

export const WorkoutSummaryHeaderWrapper = styled.div`
  display: flex;
  justify-content: center;
  margin-bottom: ${SIZES.large};
`;
